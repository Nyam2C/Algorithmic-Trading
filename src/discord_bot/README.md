# Discord Bot Commands

Discord를 통한 트레이딩 봇 원격 모니터링 및 제어

---

## 설정 방법

### 1. Discord Bot 생성

1. [Discord Developer Portal](https://discord.com/developers/applications) 접속
2. "New Application" 클릭
3. 좌측 메뉴에서 "Bot" → "Add Bot"
4. "Reset Token"으로 토큰 복사
5. Privileged Gateway Intents에서 "MESSAGE CONTENT INTENT" 활성화

### 2. Bot 초대

1. 좌측 "OAuth2" → "URL Generator"
2. Scopes: `bot`, `applications.commands`
3. Bot Permissions: `Send Messages`, `Use Slash Commands`
4. 생성된 URL로 서버에 봇 초대

### 3. 환경 변수 설정

`.env` 파일에 추가:
```bash
DISCORD_BOT_TOKEN=your_bot_token_here
DATABASE_URL=postgresql://trading:devpassword123@localhost:5432/trading

# 멀티봇 REST API 연동
TRADING_BOT_API_URL=http://localhost:8000
```

---

## 사용 가능한 명령어

### 📊 모니터링 명령어 (6개)

#### `/대시보드` (`/dashboard`) - 대시보드 UI
인터랙티브 버튼 UI로 봇 상태를 확인합니다.

**버튼 구성:**
- 📊 상태 / 📍 포지션 / 📈 통계 / 📜 내역
- ⏸️ 일시정지 / ▶️ 재시작 / 🚨 긴급청산

#### `/상태` (`/status`) - 봇 상태 확인
현재 봇의 실행 상태와 포지션 요약을 확인합니다.

**응답 내용:**
- 봇 실행 상태 (Running/Stopped/Paused)
- Uptime
- 현재 가격
- 현재 포지션 (있을 경우)
- 마지막 신호

#### `/포지션` (`/position`) - 현재 포지션 상세
현재 열려있는 포지션의 상세 정보를 확인합니다.

**응답 내용:**
- 포지션 방향 (LONG/SHORT)
- 진입 가격 및 사이즈
- TP/SL 가격
- 타임컷 남은 시간
- 현재 PnL (USD 및 %)

#### `/통계` (`/stats`) - 거래 통계
지정한 시간 동안의 거래 통계를 확인합니다.

**파라미터:**
- `hours` (선택): 조회 기간 (기본값: 24시간)

**응답 내용:**
- 총 거래 수
- 승률 (승리/패배)
- 총 손익
- 최고/최악 거래
- LONG/SHORT 비율

#### `/내역` (`/history`) - 최근 거래 내역
최근 완료된 거래 내역을 확인합니다.

**파라미터:**
- `count` (선택): 조회할 거래 수 (기본값: 5, 최대: 10)

**응답 내용:**
- 각 거래의 진입/청산 가격
- 청산 이유 (TP/SL/TIMECUT)
- PnL (USD 및 %)
- 거래 시간

#### `/계정` (`/account`) - 계정 현황
Binance Testnet 계정의 전체 포지션 및 잔고를 조회합니다.

**응답 내용:**
- USDT 잔고 (총/사용가능)
- 열린 포지션 목록
- 총 미실현 손익

---

### 🎮 제어 명령어 (4개)

#### `/일시정지` (`/stop`) - 봇 일시 정지
새로운 포지션 진입을 중지합니다.

**기능:**
- 새 포지션 진입 차단
- 기존 포지션은 유지 (TP/SL 작동)
- 재시작 전까지 WAIT 신호만 생성

#### `/재시작` (`/start`) - 봇 재시작
일시 정지된 봇을 재시작합니다.

**기능:**
- 일시 정지 해제
- 정상 거래 재개
- 일시 정지 기간 표시

#### `/긴급청산` (`/emergency`) - 긴급 청산
현재 포지션을 즉시 Market 가격으로 청산하고 봇을 정지합니다.

**기능:**
- 현재 포지션 Market 청산
- 봇 자동 일시 정지
- 긴급 상황 대응용

**주의:** 신중하게 사용하세요. Market 가격으로 즉시 청산됩니다.

#### `/핑` (`/ping`) - 응답 확인
봇이 정상적으로 응답하는지 확인합니다.

---

### 🤖 멀티봇 제어 명령어 (10개)

REST API (`http://localhost:8000`)를 통해 동작하는 멀티봇 제어 명령어입니다.

#### `/봇목록` (`/bots`) - 봇 목록 조회
등록된 모든 봇의 목록과 상태를 확인합니다.

**응답 내용:**
- 총 봇 수
- 실행 중/일시정지 봇 수
- 각 봇별 심볼, 위험도

#### `/봇상태 <bot_name>` (`/bot-status`) - 특정 봇 상태
특정 봇의 상세 상태를 조회합니다.

**파라미터:**
- `bot_name` (필수): 봇 이름 (예: `btc-conservative`)

**응답 내용:**
- 실행 상태, 심볼, 위험도, 레버리지
- 현재 가격, 루프 카운트
- 포지션 정보, 마지막 신호

#### `/봇시작 <bot_name>` (`/bot-start`) - 봇 시작
특정 봇을 시작합니다.

**파라미터:**
- `bot_name` (필수): 시작할 봇 이름

#### `/봇정지 <bot_name>` (`/bot-stop`) - 봇 정지
특정 봇을 정지합니다.

**파라미터:**
- `bot_name` (필수): 정지할 봇 이름

#### `/봇일시정지 <bot_name>` (`/bot-pause`) - 봇 일시정지
특정 봇을 일시정지합니다. 새 포지션 진입이 중지되고 기존 포지션은 계속 관리됩니다.

**파라미터:**
- `bot_name` (필수): 일시정지할 봇 이름

#### `/봇재개 <bot_name>` (`/bot-resume`) - 봇 재개
일시정지된 봇을 재개합니다.

**파라미터:**
- `bot_name` (필수): 재개할 봇 이름

#### `/전체시작` (`/start-all`) - 모든 봇 시작
등록된 모든 봇을 시작합니다.

#### `/전체정지` (`/stop-all`) - 모든 봇 정지
실행 중인 모든 봇을 정지합니다.

---

## 대시보드 UI

`/대시보드` 명령어로 인터랙티브 대시보드를 표시합니다.

```
┌────────────────────────────────────────────┐
│  🤖 트레이딩 봇 대시보드                     │
│                                            │
│  상태: ✅ 실행 중 | 가격: $95,123.45        │
│  포지션: 🟢 LONG @ $94,800.00              │
│  마지막 신호: LONG (5분 전)                 │
│                                            │
│  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐│
│  │📊 상태 │ │📍포지션│ │📈 통계 │ │📜 내역 ││
│  └────────┘ └────────┘ └────────┘ └────────┘│
│  ┌──────────┐ ┌──────────┐ ┌──────────┐    │
│  │⏸️ 일시정지│ │▶️ 재시작 │ │🚨긴급청산│    │
│  └──────────┘ └──────────┘ └──────────┘    │
└────────────────────────────────────────────┘
```

---

## 환경 변수

| 변수 | 설명 | 기본값 |
|------|------|--------|
| `DISCORD_BOT_TOKEN` | Discord 봇 토큰 | (필수) |
| `DATABASE_URL` | PostgreSQL 연결 문자열 | (필수) |
| `TRADING_BOT_API_URL` | 멀티봇 API 주소 | `http://localhost:8000` |

---

## PostgreSQL 데이터베이스

거래 기록은 PostgreSQL에 저장됩니다.

### Docker로 실행 (권장)

```bash
docker run -d \
  --name postgres-trading \
  -e POSTGRES_PASSWORD=devpassword123 \
  -e POSTGRES_DB=trading_bot \
  -p 5432:5432 \
  postgres:15
```

### 테이블 스키마

`trades` 테이블이 자동으로 생성됩니다:
- 진입/청산 정보
- PnL 데이터
- 전략 파라미터 (RSI, MA, Volume)
- 타임스탬프

---

## 보안 고려사항

### 권한 제어
- 특정 채널에서만 명령어 허용 가능
- 특정 사용자만 제어 명령어 실행 가능
- Rate limiting (권장: 1분에 10회)

### 민감 정보 보호
- API 키는 환경 변수로 관리
- 봇 토큰 노출 주의
- 프로덕션 환경에서는 HTTPS 사용

---

## 문제 해결

### 봇이 응답하지 않음
1. Discord Bot Token 확인
2. Bot이 서버에 초대되었는지 확인
3. Bot이 slash command 권한을 가지고 있는지 확인
4. 로그 확인: `logs/bot_output.log`

### 멀티봇 명령어가 동작하지 않음
1. REST API 서버 실행 확인: `curl http://localhost:8000/health`
2. `TRADING_BOT_API_URL` 환경 변수 확인
3. API 서버 로그 확인

### 데이터베이스 연결 오류
1. PostgreSQL이 실행 중인지 확인: `docker ps | grep postgres`
2. DATABASE_URL이 올바른지 확인
3. 데이터베이스가 생성되었는지 확인

### 명령어가 표시되지 않음
1. Bot을 재시작하여 명령어 sync
2. Discord 앱을 재시작
3. 서버 설정에서 Bot의 권한 확인

---

## 관련 문서

- [../api/README.md](../api/README.md) - REST API 문서
- [../../docs/SETUP_GUIDE.md](../../docs/SETUP_GUIDE.md) - 설정 가이드
- [../../db/README.md](../../db/README.md) - 데이터베이스 스키마

---

**버전:** 2.0
**마지막 업데이트:** 2026-02-03
